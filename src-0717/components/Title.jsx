const Title = (props) => {
  const titleStyle = { color: "tomato", fontSize: "32px", backgroundColor: "lime" };
  return (
    <>
      <div style={titleStyle}>List</div>
    </>
  );
};
export default Title;
